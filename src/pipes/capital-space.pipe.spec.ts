import { CapitalSpacePipe } from './capital-space.pipe';

describe('CapitalSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
